# Binaries
terraform-provider-stonebranch
*.exe
dist/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Test coverage
coverage.out

.env
.envrc
resources.test
tmp/

# Build output directory (contains sb2tf binary)
bin/

# AI assistant context
CLAUDE.md

# GitLab pipeline config lives externally (stonebranch-project repo) so it
# never ends up in a commit pushed to the GitHub fork — see that repo's
# ci/terraform-provider-stonebranch.gitlab-ci.yml
.gitlab-ci.yml
